apiVersion: v1
kind: Service
metadata:
  name: {{ include "myfirstdir.fullname" . }}
  labels:
    {{- include "myfirstdir.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myfirstdir.selectorLabels" . | nindent 4 }}
